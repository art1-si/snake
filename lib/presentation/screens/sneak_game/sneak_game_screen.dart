import 'package:flutter/material.dart';

class SneakGameScreen extends StatelessWidget {
  const SneakGameScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
